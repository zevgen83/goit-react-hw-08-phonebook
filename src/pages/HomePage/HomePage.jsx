import { HomeContainer, HomeTitle } from './HomePage.styled';

export const HomePage = () => {
  return (
    <HomeContainer>
      <HomeTitle>Phonebook welcome page</HomeTitle>
      <p>This is an app to manage phone contacts</p>
    </HomeContainer>
  );
};
